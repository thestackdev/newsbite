kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: arm64

steps:
  - name: deploy
    secrets: [base_url, api_key]
    commands:
      - echo $BASE_URL
      - echo $API_KEY
      - docker compose up -d --build --force-recreate
      - docker system prune -f -a
